<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.org.section01.xml.DynamicMapper">
    <resultMap id="menuResultMap" type="com.org.common.MenuDTO">
        <id property="code" column="MENU_CODE"/>
        <result property="name" column="MENU_NAME"/>
        <result property="price" column="MENU_PRICE"/>
        <result property="categoryCode" column="CATEGORY_CODE"/>
        <result property="orderableStatus" column="ORDERABLE_STATUS"/>
    </resultMap>

    <select id="selectMenuByPrice" parameterType="hashmap" resultMap="menuResultMap">
        select
                a.menu_code
                , a.menu_name
                , a.menu_price
                , a.category_code
                ,a.orderable_status
        from tbl_menu a
        where a.orderable_status='y'
        <if test="price gte 0 and price lte 10000">
            <![CDATA[
            and a.menu_price < #{ price }
            ]]>
        </if><!--get <=-->
        <if test="price gt 10000 and price lte 20000">
            and a.menu_price between 10000 and #{ price }
        </if>
        <if test="price gt 20000 and price lte 30000">
            and a.menu_price between 10000 and #{ price }
        </if>
        <if test="price gt 30000">
            and a.menu_price between 10000 and #{ price }
        </if>
        order by a.menu_code
    </select>
    <select id="searchMenu" parameterType="SearchCriteria" resultMap="menuResultMap">
        select
                a.menu_code
                , a.menu_name
                , a.menu_price
                , a.category_code
                ,a.orderable_status
        from tbl_menu a
        <if test="condition=='category'">
            join tbl_category b on (a.categhory_code=b.category_code)
        </if>
            where a.orderable_status='y'
        <if test="condition=='name'">
            and a.menu_name like concat('%',#{ value }, '%')
        </if>
        <if test="condition == 'category'">
            and b.category_name=#{ value }
        </if>
            order by a.menu_code
    </select>

    <select id="searchMenuBySubCategory" parameterType="SearchCriteria" resultMap="menuResultMap">
        select
            a.menu_code
            , a.menu_name
            , a.menu_price
            , a.category_code
            ,a.orderable_status
        from tbl_menu a
        where a.orderable_status='y'
        <choose>
            <when test="value == '식사'">
                and a.category_code in (4,5,6,7)
            </when>
            <when test="value == '음료'">
                and a.category_code in (8,9,10)
            </when>
            <otherwise>
                and a.category_code in (11,12)
            </otherwise>
        </choose>
        order by a.menu_code
    </select>
    <select id="searchMenuByRandomMenuCode" parameterType="hashmap" resultMap="menuResultMap">
        select
        a.menu_code
        , a.menu_name
        , a.menu_price
        , a.category_code
        ,a.orderable_status
        from tbl_menu a
        where 1 = 1
        and a.menu_code in
        <foreach collection="randomMenuCodeList" item="menuCode" open="(" separator="," close=")">
            #{ menuCode }
        </foreach>
    </select>
    <select id="searchMenuByMenuCodeOrSearchAll" parameterType="SearchCriteria" resultMap="menuResultMap">
        select
        a.menu_code
        , a.menu_name
        , a.menu_price
        , a.category_code
        ,a.orderable_status
        from tbl_menu a
        <if test="condition !=null and condition eq 'menuCode'">
            <where>
                a.menu_code = #{ value }
            </where>
        </if>
    </select>
    <select id="searchMenuByNameOrCategory" parameterType="hashmap" resultMap="menuResultMap">
        select
        a.menu_code
        , a.menu_name
        , a.menu_price
        , a.category_code
        ,a.orderable_status
        from tbl_menu a
<!--        <if test="nameValue != null">-->
<!--            where a.menu_name like concat('%',#{ nameValue }, '%')-->
<!--        </if>-->
<!--        <if test="categoryValue != null">-->
<!--            and a.category_code=#{ categoryValue }-->
<!--        </if>-->
<!--        <where>-->
<!--            <if test="nameValue != null">-->
<!--                a.menu_name like concat('%',#{ nameValue }, '%')-->
<!--            </if>-->
<!--            <if test="categoryValue != null">-->
<!--                and a.category_code=#{ categoryValue }-->
<!--            </if>-->
<!--        </where>-->
        <trim prefix="where" prefixOverrides="and|or">
            <if test="nameValue != null">
                a.menu_name like concat('%',#{ nameValue }, '%')
            </if>
            <if test="categoryValue != null">
                and a.category_code=#{ categoryValue }
            </if>
        </trim>
    </select>

    <update id="modifyMenu" parameterType="hashmap">
<!--        update tbl_menu-->
<!--            set menu_name=#{ name }-->
<!--                ,orderable_status=#{ orderableStatus }-->
<!--        where menu_code = #{ code }-->
<!--        update tbl_menu-->
<!--        <if test="name != null and name != ''">-->
<!--            set menu_name=#{ name }-->
<!--        </if>-->
<!--        <if test="orderableStatus != null and orderableStatus !=''">-->
<!--        ,orderable_status=#{ orderableStatus }-->
<!--        </if>-->
<!--        where menu_code = #{ code }-->
<!--        update tbl_menu-->
<!--        <set>-->
<!--        <if test="name != null and name != ''">-->
<!--            menu_name=#{ name }-->
<!--        </if>-->
<!--        <if test="orderableStatus != null and orderableStatus !=''">-->
<!--            ,orderable_status=#{ orderableStatus }-->
<!--        </if>-->
<!--        </set>-->
<!--        where menu_code = #{ code }-->
        update tbl_menu
        <trim prefix="set" prefixOverrides=",">
            <if test="name != null and name != ''">
                menu_name=#{ name }
            </if>
            <if test="orderableStatus != null and orderableStatus !=''">
                ,orderable_status=#{ orderableStatus }
            </if>
        where menu_code = #{ code }
        </trim>
    </update>
</mapper>